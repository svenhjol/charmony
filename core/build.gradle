plugins {
	id 'java'
}

group = 'dev.charmony.charmony'

dependencies {
    implementation project(':api')
}

// Add TOML dependencies (bundled and referenced)
addTomlDependencies(true)

// Add API dependency (bundled and referenced)
addApiDependency(true)