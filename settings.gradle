pluginManagement {
    repositories {
        maven {
            url "https://maven.fabricmc.net"
        }
        maven {
            name = "Terraformers"
            url = "https://maven.terraformersmc.com/"
        }
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = 'charmony'

// Add subprojects/modules here.
include 'core', 'api', 'ambient_sounds'