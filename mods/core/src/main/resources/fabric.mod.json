{
  "schemaVersion": 1,
  "id": "charmony",
  "version": "${mod_version}",

  "name": "${mod_name}",
  "description": "${mod_description}",

  "authors": [
    "svenhjol"
  ],
  "icon": "assets/charmony/textures/gui/sprites/charmony.png",
  "license": "MIT",
  "environment": "*",
  "accessWidener": "charmony.accesswidener",
  "entrypoints": {
    "main": [
      "svenhjol.charmony.core.common.CommonInitializer"
    ],
    "client": [
      "svenhjol.charmony.core.client.ClientInitializer"
    ]
  },

  "mixins": [
    "charmony.client.mixins.json",
    "charmony.common.mixins.json"
  ],

  "depends": {
    "fabricloader": ">=${loader_version}",
    "minecraft": "*",
    "java": ">=${java_version}",
    "fabric-api": "*",
    "charmony-api": ">=${api_version}"
  },

  "contact": {
    "homepage": "${homepage_link}",
    "sources": "${sources_link}"
  },

  "custom": {
    "modmenu": {
      "links": {
        "modmenu.discord": "${discord_link}"
      },
      "update_checker": true,
      "badges": ["library"]
    }
  }
}