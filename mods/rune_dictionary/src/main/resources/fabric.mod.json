{
  "schemaVersion": 1,
  "id": "charmony-rune-dictionary",
  "version": "${mod_version}",

  "name": "${mod_name}",
  "description": "${mod_description}",

  "authors": [
    "svenhjol"
  ],
  "icon": "assets/charmony/textures/gui/sprites/charmony-rune-dictionary.png",
  "license": "MIT",
  "environment": "*",

  "entrypoints": {
    "main": [
      "charmony.rune_dictionary.common.CommonInitializer"
    ],
    "client": [
      "charmony.rune_dictionary.client.ClientInitializer"
    ]
  },

  "depends": {
    "fabricloader": ">=${loader_version}",
    "minecraft": "*",
    "java": ">=21",
    "fabric-api": "*",
    "charmony": ">=${charmony_version}",
    "charmony-api": ">=${api_version}"
  },

  "contact": {
    "homepage": "${homepage_link}",
    "sources": "${sources_link}"
  },

  "custom": {
    "modmenu": {
      "links": {
        "modmenu.discord": "${discord_link}"
      },
      "badges": ["library"],
      "update_checker": true
    }
  }
}